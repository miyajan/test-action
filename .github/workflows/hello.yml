on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: miyajan/test-composite@master
        with:
          your_name: "Octocat"
      - name: step 1
        run: echo Workflow step 1
      - name: step 2
        run: echo Workflow step 2
